# new-cpp initialization hook
#
# You can use the following variables in this file:
# * $package       package name
# * $path          package path
# * $dependencies  package dependencies

mkdir ~/.new
cp Makefile.new ~/.new/Makefile
cp main.cpp.new ~/.new/main.cpp
